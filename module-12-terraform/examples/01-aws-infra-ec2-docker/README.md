# Demo 1 — Automate AWS Infrastructure + Deploy Docker Container on EC2

Provisions VPC + subnet + IGW + route table + security group + EC2 instance.
EC2 runs a user_data script that installs Docker + Docker Compose and starts nginx on port 80.

Maps to Module 12 demo: Automate AWS Infrastructure + deploy Docker container. fileciteturn2file0
