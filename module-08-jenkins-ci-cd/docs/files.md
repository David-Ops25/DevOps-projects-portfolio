# Files & Purpose

- `configs/jenkins/docker-compose.yml` — Run Jenkins via Docker Compose
- `pipeline/Jenkinsfile` — Example declarative pipeline
- `shared-library/vars/` — Example shared library steps
- `logs/commands.md` — Jenkins setup commands
