apiVersion: v1
kind: Secret
metadata:
  name: mosquitto-auth
type: Opaque
stringData:
  # Demo password file. For production:
  # - generate hashed password via mosquitto_passwd
  # - store in an external secret manager (Vault/Secrets Manager)
  passwordfile: |
    demo:demo-password
