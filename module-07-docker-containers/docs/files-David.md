# Files & Purpose

- `app/Dockerfile` — Node.js Dockerfile template
- `configs/docker-compose/docker-compose.yml` — Compose file for Node + MongoDB + Mongo Express
- `configs/docker-compose/.env.example` — Environment variable template
- `logs/commands.md` — Docker commands used
