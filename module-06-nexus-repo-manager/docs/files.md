# Files & Purpose

- `configs/nexus/docker-compose.yml` — Optionally run Nexus via Docker Compose
- `configs/maven/settings.xml` — Maven server credentials template
- `configs/gradle/gradle.properties` — Gradle credentials template
- `logs/commands.md` — Commands used to install/configure Nexus
