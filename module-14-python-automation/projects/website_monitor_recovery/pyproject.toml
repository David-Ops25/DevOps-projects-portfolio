[project]
name = "website_monitor_recovery"
version = "0.1.0"
description = "website_monitor_recovery"
requires-python = ">=3.10"
dependencies = ["boto3", "botocore"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
